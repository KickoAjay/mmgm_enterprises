# API Routes

Target phase: Phase 8/9 — Cashfree Integration, Orders + Tracking
Scope: Cashfree payment session creation, webhook handler, order/tracking endpoints that can't be Server Actions (e.g. webhooks needing raw request bodies).

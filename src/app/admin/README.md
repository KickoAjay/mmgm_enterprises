# Admin Panel

Target phase: Phase 11 — Admin Dashboard
Scope: `/admin` routes for dashboard, product/order/customer/coupon/inventory management. Plain folder (not a route group) because the spec requires a real `/admin` URL prefix.

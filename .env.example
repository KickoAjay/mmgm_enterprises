# Supabase — placeholders only, no project has been created yet.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Cashfree Payments — credentials deferred to the Phase 8 integration work.
CASHFREE_APP_ID=
CASHFREE_SECRET_KEY=
CASHFREE_API_URL=https://sandbox.cashfree.com/pg
CASHFREE_WEBHOOK_SECRET=

# Resend — API key deferred until a real account is connected. Sandbox
# sender only delivers to the Resend account's own registered email until
# a domain is verified.
RESEND_API_KEY=
EMAIL_FROM=MMGM Enterprises <onboarding@resend.dev>
# Inbox that gets the "new paid order" alert. Leave unset to skip that
# email entirely (no settings UI/row for this yet, just this env var).
ADMIN_NOTIFICATION_EMAIL=

# App
NEXT_PUBLIC_SITE_URL=http://localhost:3000
